import { Fragment, useReducer } from "react";

import Header from "./components/header";
import Actions from "./components/actions";
import styles from "./App.module.scss";

function App() {


  return (
    <Fragment>
      <Header account={state.account} dispatch={dispatch} />
      {state.account ? <Actions state={state} dispatch={dispatch} /> : <div className={styles.hello}> Hello AMAX</div>}
    </Fragment>
  );
}

export default App;
